{
  "version": 1,
  "default_policy": "raw_to_inbox_to_wiki",
  "roles": {
    "controller": {
      "description": "Owns approval into wiki and final synthesis.",
      "allowed_writes": ["wiki", ".kb", "outputs"]
    },
    "worker": {
      "description": "Stages sources (raw/) and drafts pages (inbox/).",
      "allowed_writes": ["raw", "inbox", ".kb"]
    },
    "reviewer": {
      "description": "Audits claims, citations, and physics correctness.",
      "allowed_writes": ["outputs/reviews"]
    }
  },
  "routing": {
    "orchestrator": "controller — planning, synthesis, architecture",
    "research_worker": "general-purpose subagents — web search + source extraction",
    "lint_triage": "deterministic, no reasoning — tools/lint.py"
  }
}
